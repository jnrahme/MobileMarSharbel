app_identifier(ENV.fetch("APP_STORE_APP_IDENTIFIER", "com.rammyinn.saintcharbel"))

apple_email = ENV["APP_STORE_CONNECT_APPLE_ID"] || ENV["FASTLANE_USER"] || "jnrahme@gmail.com"
apple_id(apple_email)

itc_team = ENV["APP_STORE_CONNECT_TEAM_ID"] || ENV["FASTLANE_ITC_TEAM_ID"] || ENV["FASTLANE_TEAM_ID"]
itc_team_id(itc_team) if itc_team && !itc_team.empty?

dev_team = ENV["APPLE_DEVELOPER_TEAM_ID"] || ENV["IOS_DEVELOPMENT_TEAM"] || ENV["FASTLANE_TEAM_ID"]
team_id(dev_team) if dev_team && !dev_team.empty?
